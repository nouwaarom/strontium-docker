* Clone strontium to this folder
~ git clone https://bitbucket.org/wildarch/strontium.git

* Build the docker image
~ docker build -t strontium/strontium .

* Run the generated image
~ docker run -it strontium/strontium
